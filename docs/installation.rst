Installation
============

Install MultiChat from PyPI:

.. code-block:: bash

   pip install scMultiChat

To install a specific version:

.. code-block:: bash

   pip install scMultiChat==0.2.0

Then check the installation:

.. code-block:: python

   import MultiChat as MC
   print(MC.__version__)
